# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
.eggs/

# Build artifacts
build/
build*/
*.so
*.o
*.a

# Generated model / engine files
*.engine
*.rtexe
*.bin
*.pkl
*.safetensors
*.npz
!tests/fixtures/cfg_reference_outputs.npz

# Cache
.cache/
.ccache/

# Editor / OS
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Long-form training run archives — only the reproducer ships.
training/_runs/**
!training/_runs/run_jax_compare.py

# Dev-local probe scripts (per-model micro-benches not part of the
# release surface). Mirror the FlashVLA dev convention.
internal-tests/
internal-docs/
notes/
third_party/cutlass
# --- Fork-local (tzutoo) ---
# These are not safe to commit upstream. The model dirs are too big; the
# opencode.json contains personal API keys.

# Model checkpoints (29 GB + 26 GB)
qwen36_fp8/
qwen36_nvfp4/

# Personal opencode config (API keys)
opencode.json
